#include "ucode.c"
int color;
main()
{
    char name[64]; int pid, cmd;

    while(1){
        pdi = getpid(); // don't do in first step, just print "in user mode" at first

        color = 0x0C;


    }
